# Safe local/CI test configuration. Never use these values in production.
NODE_ENV=test
PORT=3000
JWT_SECRET=replace-with-a-random-test-secret
DATABASE_URL=postgres://postgres:postgres@localhost:5432/motivation_calendar_test
ALLOWED_ORIGINS=http://localhost:3000
