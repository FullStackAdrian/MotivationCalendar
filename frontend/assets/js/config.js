// Runtime configuration for the static frontend.
// Set apiBaseUrl to the public backend URL when frontend and backend are hosted separately.
window.APP_CONFIG = window.APP_CONFIG || {
  apiBaseUrl: ''
};
